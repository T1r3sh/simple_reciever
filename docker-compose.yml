version: '3.8'

services:
  fastapi-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Mount data folder from host to container
      - ./data:/app/data
    env_file:
      - .env
    restart: unless-stopped
